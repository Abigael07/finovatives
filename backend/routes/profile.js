// routes/profile.js

const express = require('express');
const router = express.Router();

// You can leave this empty for now or comment out the whole file.
// When you're ready to reintroduce routes like /update or /upload-picture,
// we’ll add them back here.

module.exports = router;
